Items to complete program: 

- How to use checkbox input to enable true value, that then triggers numeric, special, uppercase, and lower case characters based upon user input. 
- How to store password length user input into variable, and generate the given number into password HTML field. 
- Needs conditional that displays error if user does not select minimum required character types. 
